<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ActionBar style -->
    <style name="PriznajActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/action_bar_background</item>
        <!-- pre kompatibilitu -->
        <item name="background">@drawable/action_bar_background</item>
        <item name="android:logo">@drawable/ic_menu_home</item>
    </style>

    <style name="PriznajActionBar.MenuTextStyle" parent="@android:style/TextAppearance.Holo">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/actionbar_text_size</item>
        <item name="android:textAllCaps">true</item>
    </style>

</resources>