<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/listview_background_gray"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >

        <android.support.v4.view.ViewPager
            android:id="@+id/activity_search_viewpager"
            android:layout_width="fill_parent"
            android:layout_height="@dimen/activity_search_viewpager" />

        <ListView
            android:id="@+id/activity_search_listview"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/listview_background_gray"
            android:cacheColorHint="@android:color/transparent"
            android:clipToPadding="false"
            android:divider="@null"
            android:dividerHeight="@dimen/listview_divider_height"
            android:fadingEdge="none"
            android:paddingBottom="@dimen/listview_padding"
            android:paddingTop="@dimen/listview_padding"
            android:scrollbars="none" >
        </ListView>
    </LinearLayout>

    <TextView
        android:id="@+id/activity_search_pager"
        style="@style/ListViewPager" />

</RelativeLayout>